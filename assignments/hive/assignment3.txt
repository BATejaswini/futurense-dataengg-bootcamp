1] select cust_id,count(*) from transactions group by cust_id;

2] select cust_id,sum(amount) from transactions group by cust_id;

3] select distinct(t.cust_id),concat(c.first_name," ",c.last_name),t.amount from transactions t join customers c on c.cust_id=t.cust_id order by amount desc limit 3;

4] select cust_id,count(*),pymt_mode from transactons group by cust_id,pymt_id;

5] select city,count(*),amount from transactions group by city,amount order by amount desc limit 3;

6]

7] select cust_id,count(*),pymt_mode from transactions group by cust_id,pymt_mode;